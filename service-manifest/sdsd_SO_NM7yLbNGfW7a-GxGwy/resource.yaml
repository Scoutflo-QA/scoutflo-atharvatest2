apiVersion: v1
kind: Service
metadata:
  name: toolitipis
  namespace: newtoo
  labels:
    app: toolitipis
spec:
  type: ClusterIP
  selector:
    app: toolitipis
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: toolitipis
  namespace: newtoo
  labels:
    app: toolitipis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: toolitipis
  template:
    metadata:
      labels:
        app: toolitipis
    spec:
      containers:
        - name: toolitipis-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: toolitipis
  namespace: newtoo
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: dont-wait-list-z4mz.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: toolitipis
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-z4mz.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
