apiVersion: v1
kind: Service
metadata:
  name: prodtest
  namespace: dev-byu
  labels:
    app: prodtest
spec:
  type: ClusterIP
  selector:
    app: prodtest
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prodtest
  namespace: dev-byu
  labels:
    app: prodtest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prodtest
  template:
    metadata:
      labels:
        app: prodtest
    spec:
      containers:
        - name: prodtest-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prodtest
  namespace: dev-byu
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-xyg4.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prodtest
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-xyg4.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
