apiVersion: v1
kind: Service
metadata:
  name: ingresscheck
  namespace: default
  labels:
    app: ingresscheck
spec:
  type: ClusterIP
  selector:
    app: ingresscheck
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingresscheck
  namespace: default
  labels:
    app: ingresscheck
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ingresscheck
  template:
    metadata:
      labels:
        app: ingresscheck
    spec:
      containers:
        - name: ingresscheck-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingresscheck
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-btm6s.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ingresscheck
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-btm6s.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
