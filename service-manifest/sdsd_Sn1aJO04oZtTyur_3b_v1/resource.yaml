apiVersion: v1
kind: Service
metadata:
  name: test-pd-pp
  namespace: default
  labels:
    app: test-pd-pp
spec:
  type: ClusterIP
  selector:
    app: test-pd-pp
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-pd-pp
  namespace: default
  labels:
    app: test-pd-pp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-pd-pp
  template:
    metadata:
      labels:
        app: test-pd-pp
    spec:
      containers:
        - name: test-pd-pp-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-pd-pp
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-yaymp.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test-pd-pp
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-yaymp.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
