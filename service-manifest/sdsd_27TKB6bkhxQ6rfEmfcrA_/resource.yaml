apiVersion: v1
kind: Service
metadata:
  name: najstatus
  namespace: dewd
  labels:
    app: najstatus
spec:
  type: ClusterIP
  selector:
    app: najstatus
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: najstatus
  namespace: dewd
  labels:
    app: najstatus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: najstatus
  template:
    metadata:
      labels:
        app: najstatus
    spec:
      containers:
        - name: najstatus-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: najstatus
  namespace: dewd
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: dont-wait-list-qc0ja.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: najstatus
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-qc0ja.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
