apiVersion: v1
kind: Service
metadata:
  name: derverververver
  namespace: test-tiecon-namespace
  labels:
    app: derverververver
spec:
  type: ClusterIP
  selector:
    app: derverververver
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: derverververver
  namespace: test-tiecon-namespace
  labels:
    app: derverververver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: derverververver
  template:
    metadata:
      labels:
        app: derverververver
    spec:
      containers:
        - name: derverververver-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: derverververver
  namespace: test-tiecon-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: dont-wait-list-ju3uj.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: derverververver
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-ju3uj.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
