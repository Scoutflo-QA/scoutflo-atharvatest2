apiVersion: v1
kind: Service
metadata:
  name: mmmlknlknl
  namespace: testtempo-cz-ud
  labels:
    app: mmmlknlknl
spec:
  type: ClusterIP
  selector:
    app: mmmlknlknl
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mmmlknlknl
  namespace: testtempo-cz-ud
  labels:
    app: mmmlknlknl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mmmlknlknl
  template:
    metadata:
      labels:
        app: mmmlknlknl
    spec:
      containers:
        - name: mmmlknlknl-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mmmlknlknl
  namespace: testtempo-cz-ud
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: nlnlknlnlnlnln.rkjangidbuilder.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mmmlknlknl
                port:
                  number: 3000
    - host: dont-wait-list-ekgi6.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mmmlknlknl
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-ekgi6.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
    - hosts:
        - nlnlknlnlnlnln.rkjangidbuilder.com
      secretName: wildcard-z07004282wlzt96eag2kr-tls
