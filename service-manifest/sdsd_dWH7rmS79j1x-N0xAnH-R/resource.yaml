apiVersion: v1
kind: Service
metadata:
  name: branchcheck
  namespace: default
  labels:
    app: branchcheck
spec:
  type: ClusterIP
  selector:
    app: branchcheck
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: branchcheck
  namespace: default
  labels:
    app: branchcheck
spec:
  replicas: 1
  selector:
    matchLabels:
      app: branchcheck
  template:
    metadata:
      labels:
        app: branchcheck
    spec:
      containers:
        - name: branchcheck-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: branchcheck
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-4qmhm.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: branchcheck
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-4qmhm.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
