apiVersion: v1
kind: Service
metadata:
  name: validchecks934
  namespace: default
  labels:
    app: validchecks934
spec:
  type: ClusterIP
  selector:
    app: validchecks934
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: validchecks934
  namespace: default
  labels:
    app: validchecks934
spec:
  replicas: 2
  selector:
    matchLabels:
      app: validchecks934
  template:
    metadata:
      labels:
        app: validchecks934
    spec:
      containers:
        - name: validchecks934-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: validchecks934
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-ict31.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: validchecks934
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-ict31.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
