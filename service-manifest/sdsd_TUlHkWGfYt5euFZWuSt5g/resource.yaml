apiVersion: v1
kind: Service
metadata:
  name: wyahii
  namespace: athunew
  labels:
    app: wyahii
spec:
  type: ClusterIP
  selector:
    app: wyahii
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wyahii
  namespace: athunew
  labels:
    app: wyahii
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wyahii
  template:
    metadata:
      labels:
        app: wyahii
    spec:
      containers:
        - name: wyahii-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wyahii
  namespace: athunew
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: dont-wait-list-yimji.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wyahii
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-yimji.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
