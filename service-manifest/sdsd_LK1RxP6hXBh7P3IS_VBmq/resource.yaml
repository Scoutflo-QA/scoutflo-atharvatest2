apiVersion: v1
kind: Service
metadata:
  name: vefververververv
  namespace: test-eso
  labels:
    app: vefververververv
spec:
  type: ClusterIP
  selector:
    app: vefververververv
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vefververververv
  namespace: test-eso
  labels:
    app: vefververververv
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vefververververv
  template:
    metadata:
      labels:
        app: vefververververv
    spec:
      containers:
        - name: vefververververv-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vefververververv
  namespace: test-eso
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: ervwetbrtbrtbrb.rkjangidbuilder.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vefververververv
                port:
                  number: 3000
    - host: dont-wait-list-eakeh.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vefververververv
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-eakeh.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
    - hosts:
        - ervwetbrtbrtbrb.rkjangidbuilder.com
      secretName: wildcard-z07004282wlzt96eag2kr-tls
