apiVersion: v1
kind: Service
metadata:
  name: magic-bool
  namespace: cert-service-test
  labels:
    app: magic-bool
spec:
  type: ClusterIP
  selector:
    app: magic-bool
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: magic-bool
  namespace: cert-service-test
  labels:
    app: magic-bool
spec:
  replicas: 1
  selector:
    matchLabels:
      app: magic-bool
  template:
    metadata:
      labels:
        app: magic-bool
    spec:
      containers:
        - name: magic-bool-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: magic-bool
  namespace: cert-service-test
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-xheo.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: magic-bool
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-xheo.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
