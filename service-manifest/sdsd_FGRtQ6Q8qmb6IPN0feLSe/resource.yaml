apiVersion: v1
kind: Service
metadata:
  name: freetrialservicedeploy1asdd
  namespace: default
  labels:
    app: freetrialservicedeploy1asdd
spec:
  type: ClusterIP
  selector:
    app: freetrialservicedeploy1asdd
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: freetrialservicedeploy1asdd
  namespace: default
  labels:
    app: freetrialservicedeploy1asdd
spec:
  replicas: 2
  selector:
    matchLabels:
      app: freetrialservicedeploy1asdd
  template:
    metadata:
      labels:
        app: freetrialservicedeploy1asdd
    spec:
      containers:
        - name: freetrialservicedeploy1asdd-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: freetrialservicedeploy1asdd
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-lw9jv.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: freetrialservicedeploy1asdd
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-lw9jv.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
