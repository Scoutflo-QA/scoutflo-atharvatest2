apiVersion: v1
kind: Service
metadata:
  name: tlsissue
  namespace: default
  labels:
    app: tlsissue
spec:
  type: ClusterIP
  selector:
    app: tlsissue
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tlsissue
  namespace: default
  labels:
    app: tlsissue
spec:
  replicas: 3
  selector:
    matchLabels:
      app: tlsissue
  template:
    metadata:
      labels:
        app: tlsissue
    spec:
      containers:
        - name: tlsissue-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tlsissue
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-s-m9a.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tlsissue
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-s-m9a.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
