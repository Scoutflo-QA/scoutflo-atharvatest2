apiVersion: v1
kind: Service
metadata:
  name: into-was
  namespace: default
  labels:
    app: into-was
spec:
  type: ClusterIP
  selector:
    app: into-was
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: into-was
  namespace: default
  labels:
    app: into-was
spec:
  replicas: 1
  selector:
    matchLabels:
      app: into-was
  template:
    metadata:
      labels:
        app: into-was
    spec:
      containers:
        - name: into-was-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: into-was
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-g-ybo.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: into-was
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-g-ybo.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
