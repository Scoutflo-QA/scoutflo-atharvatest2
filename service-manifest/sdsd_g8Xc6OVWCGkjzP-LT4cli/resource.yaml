apiVersion: v1
kind: Service
metadata:
  name: freetrialservicedeploy1ass
  namespace: default
  labels:
    app: freetrialservicedeploy1ass
spec:
  type: ClusterIP
  selector:]]]]
    app: freetrialservicedeploy1ass
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: freetrialservicedeploy1ass
  namespace: default
  labels:
    app: freetrialservicedeploy1ass
spec:
  replicas: 1
  selector:
    matchLabels:
      app: freetrialservicedeploy1ass
  template:
    metadata:
      labels:
        app: freetrialservicedeploy1ass
    spec:
      containers:
        - name: freetrialservicedeploy1ass-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: freetrialservicedeploy1ass
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistdocker-x5cme.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: freetrialservicedeploy1ass
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistdocker-x5cme.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
